var ctnBott = {
    "animIn": "slideInUp 1.2s cubic-bezier(0.77, 0, 0.175, 1)",
    "animOut": "fadeOutDown .6s cubic-bezier(0.77, 0, 0.175, 1)",
    "posF": "fixed",
    "posR": "relative"
}

/**
    class bottomNav {
        constructor(el) {
            this.el = document.querySelector('.' + el);
            this.showNavIn(el);
        }

        showNavIn(element) {
            element.style.animation = ctnBott.animIn;
        }
    }
 */